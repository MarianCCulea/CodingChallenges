/**
 * @param {number[]} nums
 * @return {number[][]}
 */
var threeSum = function(nums) {
    for (let i = 0; i < nums.length ; i++) {
        
        if(nums[i]+nums[j]==target)
        return [i,j];
        
    }
};